export interface Package {
    id: number;
    content: string; 
    update_At: string; 
    updatedAgo:string;
    numberParticipants: number; 
    levels: string[]; 
    icon: string;
    color: string;
}
